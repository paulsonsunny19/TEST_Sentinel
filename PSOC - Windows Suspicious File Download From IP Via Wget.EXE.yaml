id: 04328921-a902-4f44-8e07-3025a1781762
name: PSOC - Windows Suspicious File Download From IP Via Wget.EXE
kind: Scheduled
description: |-
  Detects suspicious file download using wget.exe
  This could be an attacker downloading tools, or second stage malware execution.
severity: Medium
queryFrequency: 5m
queryPeriod: 30m
triggerOperator: gt
triggerThreshold: 0
tactics:
- CommandAndControl
relevantTechniques:
- T1105
- T1071
query: |-
  // Detects suspicious file download using wget.exe
  // This could be an attacker downloading tools, or second stage malware execution.
  let ProcessNames = dynamic(["wget.exe"]);
  let cmd_all = dynamic(["http"]);
  let cmd_any = dynamic(["-O","--output-document"]);
  _Im_ProcessCreate(targetprocess_has_any=ProcessNames,commandline_has_all=cmd_all,commandline_has_any=cmd_any)
  | where CommandLine has_any (".bat",".dat",".dll",".exe",".gif",".hta",".jpeg",".log",".msi",".png",".ps1",".psm1",".vbe",".vbs")
  | extend
      Account = tostring(column_ifexists("ActorUsername", "")),
      AccountDomain = tostring(column_ifexists("ActorUserDomain", "")),
      Computer = tostring(column_ifexists("DvcHostname", "")),
      ProcessName = tostring(column_ifexists("TargetProcessName", "")),
      ProcessId = tostring(column_ifexists("TargetProcessId", "")),
      CommandLine = tostring(column_ifexists("CommandLine", ""))
  | where isnotempty(ProcessName)
entityMappings:
- entityType: Account
  fieldMappings:
  - identifier: Name
    columnName: Account
  - identifier: NTDomain
    columnName: AccountDomain
- entityType: Host
  fieldMappings:
  - identifier: HostName
    columnName: Computer
- entityType: Process
  fieldMappings:
  - identifier: ProcessId
    columnName: ProcessId
  - identifier: CommandLine
    columnName: CommandLine
suppressionEnabled: false
suppressionDuration: 5h
eventGroupingSettings:
  aggregationKind: SingleAlert
incidentConfiguration:
  createIncident: true
  groupingConfiguration:
    enabled: true
    reopenClosedIncident: false
    lookbackDuration: 5h
    matchingMethod: AllEntities
    groupByEntities: []
    groupByAlertDetails: []
    groupByCustomDetails: []

